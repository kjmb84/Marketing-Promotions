import { PropertyPromotionsPipe } from './property-promotions.pipe';

describe('PropertyPromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyPromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
