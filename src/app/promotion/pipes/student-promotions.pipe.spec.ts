import { StudentPromotionsPipe } from './student-promotions.pipe';

describe('StudentPromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentPromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
