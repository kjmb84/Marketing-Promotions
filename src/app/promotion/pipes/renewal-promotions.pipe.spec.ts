import { RenewalPromotionsPipe } from './renewal-promotions.pipe';

describe('RenewalPromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new RenewalPromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
