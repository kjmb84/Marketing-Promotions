import { NewLeasePromotionsPipe } from './new-lease-promotions.pipe';

describe('NewLeasePromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new NewLeasePromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
