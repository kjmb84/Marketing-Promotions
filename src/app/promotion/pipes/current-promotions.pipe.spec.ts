import { CurrentPromotionsPipe } from './current-promotions.pipe';

describe('CurrentPromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentPromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
