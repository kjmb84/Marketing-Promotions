import { MarketRatePromotionsPipe } from './market-rate-promotions.pipe';

describe('MarketRatePromotionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketRatePromotionsPipe();
    expect(pipe).toBeTruthy();
  });
});
